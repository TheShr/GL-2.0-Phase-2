name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Atlas Backend
        run: |
          curl --fail --silent --show-error https://atlas-gnn-backend.onrender.com/healthz
